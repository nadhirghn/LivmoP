this project was deployed on google cloud with compute engine instance E2 ubuntu 22 

just install jenkins in the vm, link github in the jenkins pipeline config and run the pipeline, all neccessary files are already configured in this repo.
